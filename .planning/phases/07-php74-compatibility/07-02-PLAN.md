---
phase: 07-php74-compatibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "All documentation states PHP 7.4+ as the minimum requirement"
    - "No file in the repository references PHP 8.1 as a requirement"
    - "Plugin header declares Requires PHP: 7.4"
  artifacts:
    - path: "README.md"
      provides: "Updated requirements section"
      contains: "PHP.*7.4"
    - path: "CLAUDE.md"
      provides: "Updated development notes"
      contains: "PHP 7.4"
  key_links: []
---

<objective>
Update all documentation files that reference PHP version requirements from 8.1 to 7.4, and verify no stale PHP 8.1 references remain.

Purpose: Ensures documentation accurately reflects the plugin's PHP 7.4+ compatibility after the SDK downgrade.
Output: All docs consistently state PHP 7.4+ as the minimum requirement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-php74-compatibility/07-CONTEXT.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PHP version references in all files</name>
  <files>README.md, CLAUDE.md</files>
  <action>
    First, grep the entire repository for "8.1" references to find all files that need updating:
    `grep -rn "8\.1" --include="*.md" --include="*.php" --include="*.txt" .`

    Then make these specific changes:

    1. **README.md** — In the Requirements section, change:
       `- **PHP** 8.1+`
       to:
       `- **PHP** 7.4+`

    2. **CLAUDE.md** — In the Development Notes section, change:
       `- **PHP 8.1+ required** — uses typed properties, named arguments, constructor property promotion`
       to:
       `- **PHP 7.4+ required** — compatible with PHP 7.4 through 8.x; AWS SDK v3.337.3 bundled for PHP 7.4 support`
       (The old note about typed properties/named arguments was inaccurate — the plugin code doesn't actually use PHP 8.1 features.)

    3. **ct-s3-offloader.php** — The plugin header `Requires PHP: 8.1` on line 8 should be changed to `Requires PHP: 7.4`. NOTE: If Plan 01 already updated this file, coordinate — but the header line is distinct from Plan 01's changes (constants and SDK loading), so this is safe to edit independently.

    4. Check for any other files referencing PHP 8.1 (grep results from step 1) and update them. Files in .planning/ directory should be left as-is (they are historical records).

    IMPORTANT: Do NOT modify files in .planning/ — those are historical planning artifacts. Only update user-facing documentation and plugin source files.
  </action>
  <verify>
    - `grep -rn "8\.1" --include="*.md" --include="*.php" . | grep -v ".planning/"` returns NO results referencing PHP 8.1 as a requirement
    - `grep "7.4" README.md` shows the updated requirement
    - `grep "7.4" CLAUDE.md` shows the updated development note
    - `grep "Requires PHP: 7.4" ct-s3-offloader.php` shows the updated header
    - `php -l ct-s3-offloader.php` passes syntax check
  </verify>
  <done>All user-facing documentation and the plugin header state PHP 7.4+ as the minimum requirement, with no stale PHP 8.1 references outside .planning/</done>
</task>

</tasks>

<verification>
1. `grep -rn "Requires PHP: 8.1" . --include="*.php"` returns zero results
2. `grep -rn "PHP.*8\.1" . --include="*.md" | grep -v ".planning/"` returns zero results
3. README.md requirements section says PHP 7.4+
4. CLAUDE.md development notes say PHP 7.4+
5. Plugin header says Requires PHP: 7.4
</verification>

<success_criteria>
- Every user-facing file that mentioned PHP 8.1 now says PHP 7.4
- No stale PHP 8.1 requirement references exist outside .planning/
- Plugin header `Requires PHP` field is 7.4
</success_criteria>

<output>
After completion, create `.planning/phases/07-php74-compatibility/07-02-SUMMARY.md`
</output>
